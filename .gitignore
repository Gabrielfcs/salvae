/target
**/*.rs.bk
Cargo.lock.bak

# Artefatos de planejamento (specs e planos) ficam apenas locais (não versionados)
docs/superpowers/

# Saídas do instalador (geradas pelo release; não versionar)
/packaging/Salvae-Setup.exe
/packaging/*.b3
